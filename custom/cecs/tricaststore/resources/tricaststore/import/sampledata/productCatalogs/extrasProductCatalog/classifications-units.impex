# ImpEx for Importing Units into Tricast Store

# Macros / Replacement Parameter definitions
$systemName=TricastClassification;
$systemVersion=1.0;
$systemVersion=systemVersion(catalog(id[default=$systemName]),version[default=$systemVersion]);


INSERT_UPDATE ClassificationAttributeUnit; code[unique=true]; symbol; unitType; conversionFactor[default=1]; $systemVersion[unique=true]
